# Frontend API Configuration
# For local development, this is optional - vite.config.ts proxies /api to http://localhost:4000
# For Vercel production, this can be left empty to use relative /api paths
VITE_API_BASE_URL=
